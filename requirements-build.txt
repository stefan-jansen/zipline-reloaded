# Zipline-Reloaded - Build Dependencies
# Required for building the package from source

# Core build tools
setuptools>=42.0.0
setuptools_scm[toml]>=6.2
wheel>=0.36.0

# Cython for compiling extensions
Cython>=0.29.21,<3.0

# NumPy for build (must be installed before building)
numpy>=2.0.0rc1; python_version>'3.9'
numpy<2.0; python_version<='3.9'
